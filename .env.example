# Environment Variables for RAG Application
# Copy this file to .env and update with your values
# 
# For local development (without Docker):
# - Use localhost URLs
# - Ensure services are running locally
#
# For Docker Compose:
# - These values are set automatically in docker-compose.yml
# - You typically don't need a .env file when using Docker Compose
# - But you can override values here if needed

# Ollama Configuration
# URL of the Ollama service
# Local: http://localhost:11435
# Docker: http://ollama:11435
OLLAMA_BASE_URL=http://localhost:11435

# Qdrant Vector Database Configuration
# URL of the Qdrant service
# Local: http://localhost:6333
# Docker: http://qdrant:6333
QDRANT_URL=http://localhost:6333

# Inngest Workflow Orchestration Configuration
# API base URL for Inngest
# Local: http://127.0.0.1:8288/v1
# Docker: http://inngest:8288/v1
INNGEST_API_BASE=http://127.0.0.1:8288/v1

# Inngest Authentication (optional, for local dev)
# These are set automatically in Docker Compose
# Only needed if running Inngest locally with authentication
INNGEST_EVENT_KEY=test
INNGEST_SIGNING_KEY=test
